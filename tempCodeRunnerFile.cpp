cout << endl;
            // if(turn_id == 1 ) matrix [x-1][y-1] = 1;
            // else matrix [x-1][y-1] = 2 ;
            // x-- ; y --; // giảm vì ma trận đếm từ 0
            // // if(check_logic(x,y,turn_id)) {
            // //     this -> winStatus = true ; 
            // //     cout <<  p_player1->getName() << " Win!!!" << endl ;
            // // } ;